// Add a function to return 42
// Add code to modify the addition function so both tests pass
function addition(num1, num2) {
    return num1 + num2;
}

// Add code to export the function
module.exports = addition;